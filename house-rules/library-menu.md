# House rules — `library-menu`

Rules here override the defaults in `skills/library-menu/SKILL.md`.
See [README.md](README.md) for how to write a good one.

## Seeded examples

- We have disabled `[skill]` and added private skills under `[path]` — the
  menu presented internally must reflect that set, not the upstream one.
- The menu card uses the corporate brand palette at `[path]`, not the
  library palette.

## YOUR RULES — ADD BELOW THIS LINE
