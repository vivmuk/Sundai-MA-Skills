# Deliverable Templates

Skeletons for the artefacts the workflow skills produce. They are starting
structures, not straitjackets — but the sections that carry the draft marking,
the safety scan result, and the provenance appendix are load-bearing and should
not be dropped.

Replace anything in `[square brackets]`. Delete the guidance comments (`<!-- -->`)
before delivery.
